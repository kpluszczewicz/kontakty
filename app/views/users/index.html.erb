<ul>
  <% @users.each do |user| -%>
    <li><%= user.name %> <%= link_to 'delete', user,  :method => :delete,
                                                      :confirm => I18n.t('nav.confirm')   %></li>
  <% end -%>
</ul>
<%= will_paginate @users %>

